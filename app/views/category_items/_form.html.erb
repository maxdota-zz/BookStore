<%= form_for(@category_item) do |f| %>
  <% if @category_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_item.errors.count, "error") %> prohibited this category_item from being saved:</h2>

      <ul>
      <% @category_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.number_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :book_id %><br />
    <%= f.number_field :book_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
