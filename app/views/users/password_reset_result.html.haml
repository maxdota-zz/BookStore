- if notice
  %p{ :id => "notice" } 
    = notice

- if @user 
  Account 
  = @user.username 
  's password is being reset with code 
  = @code
  = form_for(@user, :url => password_reset_result_path, :method => :post) do |f|
    .field
      = f.label :password
      = f.password_field :password
    .field
      = f.label :password_confirmation
      = f.password_field :password_confirmation
    .actions
      = f.submit "Reset password", :class => "btn btn-info"